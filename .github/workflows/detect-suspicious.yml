name: Détection automatique de matchs suspects

on:
  schedule:
    # GitHub Actions exige un cron en UTC. Toutes les 2h à :07 (hors heure
    # ronde pour éviter les retards) -> en heure de Djibouti (UTC+3) :
    # 03h07, 05h07, 07h07, 09h07, 11h07, 13h07, 15h07, 17h07, 19h07, 21h07,
    # 23h07, 01h07.
    - cron: "7 */2 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  detect:
    runs-on: ubuntu-latest
    steps:
      - name: Récupérer le code
        uses: actions/checkout@v4

      - name: Installer Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Installer les dépendances
        run: pip install -r requirements.txt

      - name: Créer le fichier .env à partir des secrets du repo
        run: |
          cat <<EOF > .env
          TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}
          FOOTBALL_API_KEY=${{ secrets.FOOTBALL_API_KEY }}
          FOOTBALL_API_BASE_URL=https://api.football-data.org/v4
          ODDS_API_KEY=${{ secrets.ODDS_API_KEY }}
          ALERT_CHAT_ID=${{ secrets.ALERT_CHAT_ID }}
          EOF

      - name: Vérifier les cotes et alerter si besoin
        run: python scripts/detect_suspicious.py

      - name: Sauvegarder l'état des cotes
        run: |
          if [ -n "$(git status --porcelain state/)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add state/
            git commit -m "Mise à jour de l'état des cotes"
            git push
          fi
