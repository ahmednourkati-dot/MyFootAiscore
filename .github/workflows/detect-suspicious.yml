name: Détection automatique de matchs suspects

on:
  # Déclenchement principal piloté depuis l'extérieur (Routines Claude ou
  # cron-job.org) via workflow_dispatch. Le cron natif ci-dessous est un
  # filet de sécurité gratuit et indépendant : lors de précédents tests il
  # ne s'était pas déclenché de façon fiable seul, mais le garder en backup
  # ne coûte rien et peut suffire si les déclencheurs externes s'arrêtent.
  workflow_dispatch:
  schedule:
    - cron: "7 */2 * * *"

permissions:
  contents: write

jobs:
  detect:
    runs-on: ubuntu-latest
    steps:
      - name: Récupérer le code
        uses: actions/checkout@v4

      - name: Installer Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Installer les dépendances
        run: pip install -r requirements.txt

      - name: Créer le fichier .env à partir des secrets du repo
        run: |
          cat <<EOF > .env
          TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}
          FOOTBALL_API_KEY=${{ secrets.FOOTBALL_API_KEY }}
          FOOTBALL_API_BASE_URL=https://api.football-data.org/v4
          ODDS_API_KEY=${{ secrets.ODDS_API_KEY }}
          ALERT_CHAT_ID=${{ secrets.ALERT_CHAT_ID }}
          EOF

      - name: Vérifier les cotes et alerter si besoin
        run: python scripts/detect_suspicious.py

      - name: Sauvegarder l'état des cotes
        run: |
          if [ -n "$(git status --porcelain state/)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add state/
            git commit -m "Mise à jour de l'état des cotes"
            git push
          fi
